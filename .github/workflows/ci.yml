name: Flag Service CI Pipeline

on:
  push:
    branches: [ develop, dev ]
  pull_request:
    branches: [ develop, dev, main, master ]

concurrency:
  group: ci-flag-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # 1. Qualidade de Código & Testes Unitários
  # Roda em todos os Pushes na develop e todos os PRs
  quality:
    name: Code Quality & Unit Tests
    uses: oseiasal/.github/.github/workflows/reusable-python-quality.yml@main
    with:
      python-version: '3.11'

  # 2. Segurança DevSecOps (SAST Bandit & SCA Trivy)
  # Roda em Push na develop e PRs para a main/master
  security:
    name: DevSecOps Security Analysis
    needs: quality
    if: |
      github.event_name == 'push' ||
      (github.event_name == 'pull_request' && (github.base_ref == 'main' || github.base_ref == 'master'))
    uses: oseiasal/.github/.github/workflows/reusable-security.yml@main
    with:
      language: 'python'
      python-version: '3.11'
      trivy-severity: 'CRITICAL,HIGH'
      fail-on-critical: true
